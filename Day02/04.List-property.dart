void main(){
  var city = ['Dhaka','Rangpur', 'Rajshahi', 'Bagura'];
  //After writing List variable name, give . (dot) to see properties of list
    var length= city.length;
    var reversed= city.reversed;
    var first =  city.first;
    var isEmpty=  city.isEmpty;
    var isNotEmpty=  city.isNotEmpty;

  print(length);
  print(reversed);
  print(first);
  print(isEmpty);
  print(isNotEmpty);
}