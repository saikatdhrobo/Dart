/* define as 'final' or 'const' keyword.
  
  */

  void main(){
    const thisYear = 2025;
    print(thisYear);
    // thisYear = 2030;
    // print(thisYear); //Can't re-assign to the const variable as 2025 is a constant defined here
  }

  /* 
  final = fixed later (runtime) such as final time = DateTime.now();
  const = fixed always (compile time) such as const pi = 3.14;
 */